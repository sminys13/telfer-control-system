# WIRING.md — подключение оборудования

Проект: **telfer-control-system** (Arduino Mega 2560, 2 тельфера, 4 частотных преобразователя по Modbus RTU).

## 0) Общие правила

### 0.1 Общая земля
**У всех источников питания и всех модулей должен быть общий GND.**

Если питание раздельное (например 12V→3.3V для лазеров/дисплея и USB 5V для Arduino), всё равно:
- GND(3.3V преобразователя) = GND(Arduino) = GND(MAX485) = GND(датчиков)

### 0.2 INPUT_PULLUP (как подключать кнопки/концевики)
В прошивке дискретные входы настроены как `INPUT_PULLUP`.
Это значит:
- внутри Arduino включается подтяжка к +5V (~20…50 кОм),
- **контакт/кнопка подключается между входом и GND**,
- «активный» уровень обычно **LOW**.

**NO (нормально-разомкнутый) контакт**
- в покое: вход HIGH,
- нажали: замкнулось на GND → вход LOW (событие).

**NC (нормально-замкнутый) контакт**
- в покое: вход замкнут на GND → LOW,
- сработал/оборвался провод: контакт разомкнулся → вход HIGH.

> Для безопасности NC предпочтительнее: обрыв провода тоже детектируется как авария.

### 0.3 Нужны ли внешние резисторы?
Для макетки — **обычно нет**, достаточно встроенных подтяжек Arduino.

Для длинных проводов/помех на объекте можно усилить помехоустойчивость:
- внешняя подтяжка 4.7…10 кОм к +5V (если вход должен быть HIGH в покое),
- RC-фильтр: 10 кОм + 100 нФ (вход→GND),
- экранированный кабель, правильная прокладка, общая земля «звездой».

## 1) Питание

### 1.1 Рекомендуемая схема питания (как у тебя на столе)
- БП 12V 2A
- DC-DC до 3.3V → **лазерные дальномеры + дисплей**
- между 3.3V и GND: **470 мкФ** (и желательно +100 нФ рядом с дисплеем)
- USB ПК → Arduino Mega 5V → **прочие 5V модули** (HC-SR04, клавиатура)

> Важно: земли 12V/3.3V/USB обязаны быть общими.

## 2) Дисплей GMG12864-06D (ST7565R, SPI)

### 2.1 Подключение (как у тебя)
- **CS**  → D10
- **RSE/RST** → D9
- **RS (A0/DC)** → D8
- **SCL (SCK)** → D52
- **SI (MOSI)** → D51
- **VDD** → 3.3V
- **VSS** → GND

Подсветка:
- **A (анод)** → через резистор к питанию
- **K (катод)** → GND

Резистор подсветки:
- если от **5V**: обычно 100…220 Ω (ты поставил 100 Ω — это ОК для проверки)
- если от **3.3V**: обычно 47…150 Ω

Неиспользуемые выводы (IC_SCL/IC_CS/IC_SO/IC_SI) не подключаем.

## 3) RS-485 (MAX485) + 4 частотника

### 3.1 MAX485 к Arduino Mega
Используем Serial1 (аппаратный UART):
- Mega **TX1 (D18)** → MAX485 **DI**
- Mega **RX1 (D19)** ← MAX485 **RO**
- Mega **D6** → MAX485 **DE** и **RE** (обычно их соединяют вместе)
- MAX485 **VCC** → 5V
- MAX485 **GND** → GND

Рекомендации по шине:
- На одном из концов линии поставить терминатор **120 Ω** между A и B.
- Если линия короткая (макетка) — может работать и без терминатора, но на объекте лучше поставить.

### 3.2 RS-485 к частотникам
Подключение клемм зависит от маркировки частотника:
- у тебя: **A- → B(MAX485)**, **A+ → A(MAX485)**.

Главное правило: все устройства на одной линии A к A, B к B (не перепутать полярность).

### 3.3 Адреса частотников в проекте
В проекте принято:
- H1 = адрес 1
- H2 = адрес 2
- V1 = адрес 3
- V2 = адрес 4

(порядок жёстко задан в `src/motors.cpp`.)

## 4) Лазерные дальномеры (UART 3.3V)

Используются два UART:
- Laser1 = Serial2 (Mega RX2=D17, TX2=D16)
- Laser2 = Serial3 (Mega RX3=D15, TX3=D14)

### 4.1 Сигнальные уровни
Лазеры питаются от **3.3V** и имеют **3.3V TTL UART**.

- **Лазер TX (3.3V)** → Mega RX (5V) можно подключать напрямую: 3.3V распознаётся как HIGH.
- **Mega TX (5V)** → Лазер RX (3.3V) нужно понизить уровни.

Самый простой вариант — делитель напряжения на 2 резисторах.

**Пример делителя 5V → ~3.3V:**
- Rверх = 10 кОм (между Mega TX и Лазер RX)
- Rниз = 20 кОм (между Лазер RX и GND)

Можно также 4.7 кОм / 10 кОм.

### 4.2 Подключение Laser1
- Mega **TX2 (D16)** → (через делитель) → Laser1 **RX**
- Mega **RX2 (D17)** ← Laser1 **TX**
- Laser1 **VCC** → 3.3V
- Laser1 **GND** → GND

### 4.3 Подключение Laser2
- Mega **TX3 (D14)** → (через делитель) → Laser2 **RX**
- Mega **RX3 (D15)** ← Laser2 **TX**
- Laser2 **VCC** → 3.3V
- Laser2 **GND** → GND

## 5) Ультразвуковые датчики HC-SR04 (5V)

Питание HC-SR04: **5V**.

Подключение (как в проекте):
- US1: TRIG → D22, ECHO → D23
- US2: TRIG → D24, ECHO → D25
- VCC → 5V
- GND → GND

Дополнительно (желательно): 100 нФ между VCC и GND рядом с датчиком.

## 6) E-STOP и концевики (горизонтальные, NC)

Пины в проекте (по умолчанию):
- E-STOP: D2
- Концевики H1 LEFT/RIGHT, H2 LEFT/RIGHT: D36..D39 (см. `include/config.h`)

Подключение с `INPUT_PULLUP`:
- Один вывод контакта → **GND**
- Второй вывод контакта → **входной пин Arduino**

Для NC в реальной схеме:
- в норме контакт замкнут, вход LOW,
- при срабатывании/обрыве → вход HIGH.

## 7) Матричная клавиатура 4x4 (вместо россыпи кнопок)

Используется 8 линий: C1..C4, R1..R4.

Пины по умолчанию (можно менять в `include/config.h`):
- C1 → A0
- C2 → A1
- C3 → A2
- C4 → A3
- R1 → A4
- R2 → A5
- R3 → A6
- R4 → A7

Подключение:
- Клавиатура C1..C4 → соответствующие Arduino пины
- Клавиатура R1..R4 → соответствующие Arduino пины

Никаких внешних резисторов не нужно: строки читаются как `INPUT_PULLUP`.

### 7.1 Раскладка клавиш (как сейчас в прошивке)

Меню:
- `2` = вверх
- `8` = вниз
- `A` = Enter
- `B` = Back
- `C` = Status (вернуться на главный экран)

Ручной режим (MANUAL):
- H1 влево  = `4`   | H1 вправо = `6`
- H2 влево  = `1`   | H2 вправо = `3`
- H sync (оба вместе, если включено) влево=`2`, вправо=`8`
- V1 вверх  = `7`   | V1 вниз   = `9`
- V2 вверх  = `*`   | V2 вниз   = `#`
- Стоп (всегда) = `D`

> Напоминание: **H Forward = вправо**, **V Forward = вниз**.

## 8) Энкодер (опционально, если вернёмся к нему)

Твой энкодер-модуль EC11:
- S1 → D3
- S2 → D4
- KEY → D5
- 5V → 5V
- GND → GND

Если «пропуски»/дребезг:
- можно добавить аппаратный RC-фильтр (например 10 кОм + 10…100 нФ) на линии S1/S2,
- или увеличить деление/антидребезг в софте.

## 9) Проверка частотника без двигателя

Обычно частотник можно включать и проверять **без подключенного двигателя**, но:
- на выходе частотника будет опасное напряжение — **не трогать клеммы U/V/W**,
- некоторые модели могут ругаться на отсутствие нагрузки/непройденное автоопределение, но чаще просто показывают частоту.

Самый безопасный тест связи (без запуска мотора):
1) Подай питание на частотник.
2) Подключи RS-485.
3) На экране Arduino в статусе смотри строку `MB H1? H2? V1? V2?`:
   - `O` = связь есть,
   - `-` = нет связи,
   - `E` = связь есть, но частотник в FAULT.

Тест с запуском (если хочешь):
- задай частоту маленькую (например 5 Гц) и дай RUN со стороны панели частотника,
- убедись, что частотник меняет состояние, а Arduino видит связь.

> На объекте обязательно делайте первый запуск с двигателем и корректными параметрами мотора.
