# WIRING.md — telfer-control-system (Arduino Mega 2560)

Этот документ — **практическая инструкция по подключению** на макетке и в шкафу.

## 0) Ключевые принципы

1) **Mega — 5V логика**, а дисплей ST7565R и лазерные дальномеры — **3.3V**.
   Поэтому линии **Mega → дисплей/лазеры** должны быть **понижены до 3.3V**.

2) На макетке (короткие провода) можно использовать **внутренние подтяжки** Arduino (`INPUT_PULLUP`).
   В шкафу (длинные провода/помехи от ПЧ) рекомендуется **внешняя подтяжка 4.7k–10k** и RC-фильтрация.

3) Для цепей безопасности (E-STOP, концевики) в промышленном варианте лучше **NC (нормально замкнутые)** контакты:
   - Норма: вход удерживается в «безопасном» состоянии.
   - Обрыв провода = авария (fail-safe).

4) ВНИМАНИЕ по питанию 3.3V:
   - Дисплей + 2 лазера + подсветка могут потреблять суммарно >200 мА.
   - Если подсветка подключена через слишком маленький резистор — 3.3V может просаживаться и вызывать «гашение» дисплея/лазеров.

---

## 1) Таблица пинов (как в `include/config.h`)

### 1.1 Дисплей ST7565R (SPI)
| Сигнал | Mega |
|---|---:|
| SCK (SCL) | D52 |
| MOSI (SI) | D51 |
| CS | D10 |
| DC (RS) | D8 |
| RST (RSE/RES) | D9 |
| VDD | 3.3V |
| VSS | GND |
| A (подсветка +) | через резистор к 3.3V |
| K (подсветка −) | GND |

> Пины `IC_SCL / IC_CS / IC_SO / IC_SI` на вашем модуле можно НЕ подключать — это альтернативный интерфейс/разводка.

### 1.2 Матричная клавиатура 4x4 (основное управление на макетке)
| Пин клавиатуры | Mega |
|---|---:|
| C1 | A0 |
| C2 | A1 |
| C3 | A2 |
| C4 | A3 |
| R1 | A4 |
| R2 | A5 |
| R3 | A6 |
| R4 | A7 |

> В прошивке (fixed5): `USE_KEYPAD=1`, `USE_ENCODER=0`.

### 1.2b Энкодер EC11 (опционально, если захотите вернуть)
| Пин модуля | Назначение | Mega |
|---|---|---:|
| S1 | CLK | D3 |
| S2 | DT | D4 |
| KEY | SW (кнопка) | D5 |
| VCC | +5V | 5V |
| GND | 0V | GND |

### 1.3 RS-485 (MAX485) — Modbus RTU
Используется `Serial1`:
| MAX485 | Mega |
|---|---:|
| DI | TX1 (D18) |
| RO | RX1 (D19) |
| DE | D6 |
| RE | D6 (вместе с DE) |
| VCC | 5V |
| GND | GND |
| A | линия RS-485 A |
| B | линия RS-485 B |

### 1.4 Лазерные дальномеры (UART 3.3V)
| Лазер | TX (датчик) -> RX Mega | RX (датчик) <- TX Mega | Питание |
|---|---:|---:|---|
| Laser1 | RX2 (D17) | TX2 (D16) через понижение уровня | 3.3V |
| Laser2 | RX3 (D15) | TX3 (D14) через понижение уровня | 3.3V |

### 1.5 HC-SR04 (5V)
| Датчик | TRIG | ECHO | VCC |
|---|---:|---:|---:|
| US1 | D22 | D23 | 5V |
| US2 | D24 | D25 | 5V |

### 1.6 Безопасность
| Сигнал | Mega | Тип |
|---|---:|---|
| E-STOP | D2 | предпочтительно NC |
| H1 LEFT | D36 | NC |
| H1 RIGHT | D37 | NC |
| H2 LEFT | D38 | NC |
| H2 RIGHT | D39 | NC |

### 1.7 Кнопки
Все кнопки рассчитаны на схему: **пин —(кнопка)— GND** и в коде `INPUT_PULLUP`.

| Функция | Mega |
|---|---:|
| STOP | D26 |
| START | D27 |
| H_BOTH_FWD (опц.) | D28 |
| H_BOTH_BWD (опц.) | D29 |
| V1_UP | D30 |
| V1_DOWN | D31 |
| H1_FWD | D40 |
| H1_BWD | D41 |
| H2_FWD | D42 |
| H2_BWD | D43 |
| V2_UP | D44 |
| V2_DOWN | D45 |

---

## 2) Дисплей: уровни и подсветка

### 2.1 Уровни SPI (5V → 3.3V)
Mega отдаёт 5V на SCK/MOSI/CS/DC/RST. Для ST7565R на 3.3V это риск.

**Рекомендовано (лучше всего):**
- 74HC4050 или 74LVC125/74LVC245 (питание 3.3V) — как level shifter 5V→3.3V.

**Допустимо на макетке (коротко):** делители на каждую линию (SCK, MOSI, CS, DC, RST)
- R1 = 1 kΩ (от пина Mega к входу дисплея)
- R2 = 2 kΩ (от входа дисплея на GND)

**Дополнительно против звона:**
- 33–100 Ω последовательно у Mega на SCK и MOSI.

### 2.2 Подсветка A/K
Вы подключили:
- A → через 10 Ω к 3.3V
- K → GND

Это **может быть слишком малым резистором** и перегружать 3.3V питание (особенно если тем же 3.3V питаются лазеры).

**Рекомендация:**
- Начните с **100 Ω**, если темно — 68 Ω, если ярко — 150–220 Ω.
- Если на модуле уже есть встроенный резистор подсветки, внешний резистор может быть не нужен.

Симптомы слишком большой нагрузки подсветкой:
- дисплей «гаснет»/перезагружается,
- лазеры теряют луч/точку,
- помогает «передёрнуть» питание.

---

## 3) Что значит `INPUT_PULLUP` и нужны ли внешние резисторы

### 3.1 Внутренние подтяжки Arduino
`pinMode(pin, INPUT_PULLUP)` включает **встроенный резистор подтяжки к +5V** (типично ~20–50 kΩ).

Это означает:
- В норме вход = HIGH.
- При нажатии кнопки (замыкание на GND) вход = LOW.

**На столе** это обычно достаточно.

### 3.2 Внешние подтяжки (рекомендуется в шкафу)
Для длинных проводов и помех (ПЧ, моторы) внутренней подтяжки часто мало.

Рекомендуется:
- **4.7k–10k** к +5V на вход,
- + конденсатор **10–100 nF** к GND возле Mega (RC-фильтр).

### 3.3 У вас NO-кнопки вместо NC (E-STOP/концевики)
Для **E-STOP и концевиков** промышленно правильнее NC.

На макетке можно:
- либо **перемычкой замкнуть** вход «как NC»,
- либо временно перейти на NO-логику:
  - в `include/config.h` включить режим макетки:
    - `BENCH_MODE = true`
  - и подключить NO-кнопку как: **пин —(кнопка)— GND**.

Пояснение:
- В штатной машине (NC) авария = **обрыв** (вход становится HIGH из-за `INPUT_PULLUP`).
- На столе (NO) авария = **нажатие** (вход становится LOW). Именно это делает `BENCH_MODE=true`.

⚠️ Для реальной эксплуатации вернитесь к NC.

---

## 4) Управление на макетке: матричная клавиатура 4x4

В этой версии проекта (fixed5) управление меню и ручным режимом делается **через матричную клавиатуру 4x4**
(потому что энкодер на Mega часто ведёт себя нестабильно, если один канал сидит не на пине с прерываниями).

### 4.1 Подключение
Пины см. в разделе **1.2**: C1..C4 → A0..A3, R1..R4 → A4..A7.

В прошивке:
- R1..R4 настроены как `INPUT_PULLUP` (внутренние подтяжки к +5V).
- C1..C4 — `OUTPUT`, в сканировании по очереди опускаются в `LOW`.

> Желательно нажимать **одну кнопку за раз** (у большинства 4x4 без диодов возможны «фантомные» нажатия при нескольких одновременных).

### 4.2 Раскладка
Стандартная 4x4:

```
1 2 3 A
4 5 6 B
7 8 9 C
* 0 # D
```

### 4.3 Навигация по меню
- `A` — MENU / ENTER
- `B` — BACK
- `2` — UP
- `8` — DOWN
- `C` — STATUS (домой на экран статуса)
- `D` — STOP (всегда, для безопасности)

### 4.4 Ручной режим (MANUAL) — временное управление движениями
- H1: `4` = left (bwd), `6` = right (fwd)
- H2: `1` = left (bwd), `3` = right (fwd)
- V1: `7` = up, `9` = down
- V2: `*` = up, `#` = down
- BOTH (горизонталь синхронно): `2` = both left, `8` = both right — **только если** в Settings включено `Manual H-sync = ON`.

---

## 5) RS-485 (MAX485): терминатор и bias

### 5.1 DE/RE
- DE и RE объединены и идут на D6.
- Рекомендуется подтянуть D6 к GND через **10 kΩ**, чтобы при старте был режим приёма.

### 5.2 Терминация
- 120 Ω **между A и B** — только на концах линии.

### 5.3 Bias (если линия «плавает»)
Если в оборудовании нет bias-подтяжек:
- A → +5V через 680 Ω…1 kΩ
- B → GND через 680 Ω…1 kΩ

### 5.4 Частая ошибка
Если связи нет — попробуйте **поменять местами A/B** (маркировки часто путают).

---

## 6) Лазерные дальномеры: уровни UART и почему может исчезать точка

### 6.1 Понижение уровня TX Mega → RX лазера
Обязательно понизить 5V до 3.3V на линиях:
- D16 (TX2) → RX лазера1
- D14 (TX3) → RX лазера2

Делитель:
- R1=1 kΩ (последовательно от TX Mega)
- R2=2 kΩ (от входа лазера на GND)

### 6.2 Почему «луч был, а потом пропал»
Возможные причины:
1) **Просадка 3.3V** (часто из-за подсветки дисплея или слабого 3.3V преобразователя).
2) Дальномер выключает луч/измерения без команд.

В прошивке на старте отправляются команды:
- включить луч,
- включить непрерывные измерения.

Если ваш дальномер настроен иначе — команды будут проигнорированы.

---

## 7) Можно ли подключать ПЧ без двигателя для проверки?

Коротко: **проверять связь по Modbus — да**, «крутить» без двигателя — обычно не нужно.

### 7.1 Что можно безопасно проверить без двигателя
- Настройки ПЧ (адрес, скорость, parity).
- Чтение/запись регистров (например команда STOP/RESET, задание частоты).
- Реакцию ПЧ на команду: на дисплее ПЧ может изменяться «задание/частота», может загораться RUN-индикатор.

### 7.2 Что может произойти, если дать RUN без двигателя
Многие ПЧ разрешают RUN без нагрузки, но:
- ПЧ может уйти в аварию «обрыв двигателя/нет тока» или аналог.
- На выходных клеммах ПЧ появляется опасное напряжение.

**Рекомендация для стола:**
- Подключите RS-485 и питание ПЧ.
- Не трогайте силовой выход, не включайте RUN на длительное время.
- Для проверки «управления» смотрите:
  - меняется ли задание частоты,
  - реагирует ли статус по Modbus,
  - нет ли ошибок связи.

---

## 8) Быстрый чек-лист, если что-то не работает

### 8.1 Дисплей
- Проверьте 3.3V под нагрузкой (подсветка + лазеры). Если <3.2V — уменьшайте нагрузку.
- Проверьте уровни SPI (желательно level shifter).

### 8.2 Энкодер
- Убедитесь, что S1/S2/KEY соединены с D3/D4/D5 и есть общий GND.
- Поменяйте S1 и S2 местами, если направление «наоборот».

### 8.3 Лазеры
- Проверьте 3.3V, общий GND.
- Проверьте делитель на TX Mega.

### 8.4 RS-485
- Поменяйте A/B.
- Убедитесь, что DE/RE управляется (D6).
- Проверьте совпадение baud/parity.

